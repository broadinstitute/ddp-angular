import { AssigneesComponent } from './assignee.component';

describe('Component: Assignees', () => {
  it('should create an instance', () => {
    const component = new AssigneesComponent();
    expect(component).toBeTruthy();
  });
});
