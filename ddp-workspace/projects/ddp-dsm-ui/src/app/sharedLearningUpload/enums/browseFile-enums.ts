export enum UploadButtonText {
  UPLOAD = 'Upload',
  UPLOAD_SUCCESS = 'Uploaded',
  UPLOAD_FAIL = 'Upload failed',
  UPLOAD_IN_PROGRESS = 'Uploading...',
  UPLOAD_RETRY = 'Retry'
}

export enum UploadStatus {
  NONE,
  SUCCESS,
  FAIL,
  IN_PROGRESS,
  RETRY
}
